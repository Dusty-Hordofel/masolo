/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "res.cloudinary.com",
      "www.nike.com",
      "uploadthing.com",
      "images.unsplash.com",
    ],
  },
};

export default nextConfig;
